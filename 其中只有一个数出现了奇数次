#include<stdio.h>
#include<string.h>
#include<stdlib.h>

int arr[1000000];
int main()
{
	int t;
	scanf("%d",&t);
	while(t--)
	{
		int n;
		scanf("%d",&n);	
		int i,ret=0;
		for(i=0;i<n;i++)
		{
			scanf("%d",&arr[i]);		
		}
		
		for(i=0;i<n;i++)
		{
			ret^=arr[i]; 
		}
		printf("%d\n",ret);
		
	}	  
	return 0;
}
