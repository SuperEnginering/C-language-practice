#include <stdio.h>
#include <string.h>

int main(){
	int T;
	scanf("%d",&T);
	while(T--)
	{
		//put
		int N;
		scanf("%d",&N);
		int i;
		int a[10000]={0};
		for(i=0;i<N;i++)
		{
			scanf("%d",&a[i]);
		}
//		int q[5000]={0};
		int j;
		int Q;
		scanf("%d",&Q);
		while(Q--)
		{
			int q;
			scanf("%d",&q);
			int u;
			int flag=0;
			for(u=0;u<N;u++)
			{
				if(q==a[u]) flag=1;
			} 
			if(flag==1) printf("Yes\n");
			else if(flag==0) 	printf("No\n");		
		}
		//查询
		 
	}	
	return 0;
}
