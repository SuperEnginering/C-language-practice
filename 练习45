#include <stdio.h>
#include <string.h>

int f(int n)
{
	if(n==1) return 1;
	else if( n==2 ) return 2;
	
	else return f(n-1)+f(n-2);	
}

int main()
{ 
	int T;
	scanf("%d",&T);
	while(T--)
	{
		int N;
		scanf("%d",&N);
		printf("%d\n",f(N));
		
	}
    return 0;
}


